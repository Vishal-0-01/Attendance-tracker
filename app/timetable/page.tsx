'use client';import {Shell} from '@/components/Shell';import {TimeEdit} from '@/components/Setup';import {id} from '@/lib/store';
export default function Page(){return <Shell>{({data,setData})=><div className="grid gap-6"><h1 className="text-3xl font-bold">Timetable</h1><div className="card p-4"><TimeEdit data={data} setData={setData} addL={()=>setData(d=>({...d,timetable:[...d.timetable,{id:id(),day:'Monday',start:'09:00',end:'10:00',subjectId:d.subjects[0]?.id||''}]}))}/></div><p className="muted">Use History to add one-time extra, moved, or cancelled lectures without changing this weekly timetable.</p></div>}</Shell>}
